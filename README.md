# popos-workspace-config
My Pop!_OS Workspaces config to emulate i3

This script turns off dynamic workspaces and creates 4 static workspaces that can be navigated using the Super key + workspace number.
